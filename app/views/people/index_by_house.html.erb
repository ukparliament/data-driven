<h1>Most active members of the <%= @house[:label] %> based on the number of total contributions</h1>
<ul>
	<% @house[:members].each do |member| %>
		<li>
			<%= link_to person_path(member[:id]) do %>
				<%= member[:name] %> (<%= member[:count] %>)
			<% end %>
		</li>
	<% end %>
</ul>